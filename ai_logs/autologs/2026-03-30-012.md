# AIAD 自动开发记录

### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 排查并修复“关键词串数据/返回上一个关键词内容”的问题，确保每次任务只使用本次抓取数据。
- **[Prompt]**: 用户反馈 processed/raw 出现上一个 keyword 内容，重新登录后恢复，要求定位根因并彻底修复。
- **[Rules Applied]**: 使用指定 conda 环境执行验证；先读取真实 raw/processed 与 tasks 证据再改代码；先落盘 Auto-Log 再回复。
- **[Skills Used]**: FastAPI 服务链路排障、MediaCrawler 输出判定、关键词一致性校验、任务隔离目录策略、Ruff、Mypy、Pytest。
- **[Dev Process/Iterations]**: 先验证 `c19f58ec6282` 的 raw 实际 `source_keyword=美食` 与任务关键词 `挽救计划` 不一致；确认根因是“全局取最新文件+回退策略”导致串数据；实现任务级输出目录 `data/raw/_runs/{task_id}` 与关键词一致性校验，不匹配即失败并提示重新登录；最终 lint/typecheck/test 全通过。
