### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 运行MediaCrawler爬虫，抓取20个《流浪地球》相关的小红书帖子及评论（每个15条），并整理为下游Agent所需的标准State格式。
- **[Prompt]**: 测试一下，尝试找一下20个帖子，每个帖子15条评论，话题是《流浪地球》，平台是小红书，要求保留图片，我需要这个数据给后面的agent使用。
- **[Rules Applied]**: 强制开发日志记录(Auto-Logging)、系统解耦设计、使用项目指定的Conda环境（`.conda\aiad\python.exe`）。
- **[Skills Used]**: MediaCrawler, Python, JSON Parsing, 数据清洗与格式化.
- **[Dev Process/Iterations]**: 
  1. 分析了项目中已有的 `MediaCrawler` 及 `app.services.crawler_runner`。
  2. 编写了 `scripts/test_crawler.py` 脚本，配置了关键词 `流浪地球`、数量 `limit=20`、`max_comments_per_note=15` 以及 `enable_media_download=True`，并调用运行。
  3. 爬虫成功执行并输出到了 `data/raw/_runs/{task_id}/xhs/` 目录。
  4. 根据上一轮定义的《智能体接口与并行开发规范》，编写了数据整理脚本 `scripts/format_data_for_agents.py`。
  5. 将原始的 JSONL 格式数据和分散的图片，重新清洗、组装，按规范存入 `e:/AIAD/data/raw/{post_id}/`，生成了包含图片的 `media/` 目录、提取的 `comments.json` 和直接可用的 `mock_state.json`。
  6. 数据现已完美就绪，Agent 2 ~ Agent 5 可以直接读取这些 `mock_state.json` 开始本地并行开发了。